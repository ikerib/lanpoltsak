<form method="post" action="{{ path('type_delete', {'id': type.id}) }}" onsubmit="return confirm('Ziur zaude ezabatu nahi duzula?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button id="formBtnRemove" class="btn">{{ "Ezabatu" | trans }}</button>
</form>
